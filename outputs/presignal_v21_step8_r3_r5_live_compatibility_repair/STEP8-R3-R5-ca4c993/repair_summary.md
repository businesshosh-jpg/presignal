# Step 8-R3-R5 Live Compatibility Repair

The original smoke is immutable. A child contract was bound to the replacement smoke. Its compact Anthropic Attention rule and explicit Request enums reached the live provider payloads.

The one permitted replacement smoke did not clear the live gate. Anthropic still ended in a provider-side JSON parse error under the generic 4096-token bridge ceiling. Gemini emitted `primary_driver` and `secondary_driver` in the Request `priority` field. OpenAI emitted `other` for one Request `affected_channel`. All remained strict rejections; no aliases were accepted, no forecast call was made, and resume made zero calls.
