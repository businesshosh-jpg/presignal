# PreSignal v2.1 R6 live-scope binding

This versioned binding preserves the V1 Gemini paired Pack A/E experiment: one prospective Episode, two calls, and zero retries. It does not execute R6 or alter Route B compute.

Repository evidence does not bind an immutable prospective approved-source environment or a bounded Google input/evidence target. The Move 4C source environment is test-only. The historical AKSR registry is preserved as historical capability provenance, not prospective R6 authority. The legacy benchmark workbook is rejected because its writer reaches `Outcome_Comparison`; it cannot prove isolated paired-R6 writes.

The resulting V2 identity is a deterministic blocked-scope binding. Explicit user authorization must identify the prospective source environment and existing isolated Google input and evidence targets before execution can be authorized.
