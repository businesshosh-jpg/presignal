# PreSignal v2.1 R6 native input materialization

The return-only native Attention adapter validates an already-authoritative accepted selected Attention result. It does not decide selection: the stable v2 Attention contract is provider-response-driven. With zero provider calls authorized and no frozen current Episode response, no live selected Attention may be manufactured.

Canonical Pack A is traced to the stable v2 provider-owned ordered Information Request set. It contains neither acquired source content nor Pack E lineage. Therefore it cannot be built for a live Episode until selected Attention and canonical Requests exist.

The attempted bounded Google structural setup did not reach Sheets because the in-memory OAuth refresh failed at transport level; the token file checksum remained unchanged. No native object rows, R6 evidence, forecast, Outcome, or evaluation were written.
