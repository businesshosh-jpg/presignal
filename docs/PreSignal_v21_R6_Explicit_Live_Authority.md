# PreSignal v2.1 R6 explicit live authority

`AKSR_V1_INITIAL_REGISTRY` is bound only as `PRESIGNAL_V21_R6_PROSPECTIVE_SOURCE_ENVIRONMENT_V1` for one R6 paired smoke. This does not promote the registry into general prospective authority or permit source expansion.

Controlled operational drift created one workbook, `presignal_v21_r6_paired_smoke_evidence`, with one `R6_Paired_Evidence` sheet and a fixed 32-field paired-evidence schema. Its writer is caller-controlled, has a one-record limit, and rejects Outcome/evaluation fields. Route B compute is unchanged.

The main prospective workbook provides the canonical `Event` source, but its inspected objects do not provide native accepted selected Attention or canonical Pack A inputs. Accordingly the V3 authority remains blocked; no R6 execution is authorized.
