# PreSignal v2.1 R6 paired Pack A/E authorization

This authorization binds one future prospective Episode, Gemini / `gemini-2.5-flash-lite`, and exactly two paired forecast arms: Pack A followed by Pack E. It permits no retries, provider or model substitution, historical or synthetic Episode, Outcome, evaluation, or production promotion.

It binds `PRESIGNAL_V21_ROUTE_B_CAPABILITY_FREEZE_V1` at `sha256:8c910a343515d88ca63ce4aaf738f28f9b4a8ab22665397077858bfaf7e866e4`. The frozen historical proof establishes preservation of historical shared-Pack compute; the native proof establishes the prospective return-only contract. Neither authorizes provider dispatch, Google access, acquisition, forecast execution, Outcome construction, or evaluation.

The present authorization is blocked: no immutable approved-source environment is bound to the prospective R6 scope in repository evidence. Google input, Google evidence-write, and final evidence-destination bindings also remain unresolved and are recorded without selecting a legacy benchmark target. A separate authorization must bind those exact live boundaries before R6 can execute.
